# Test Environment Variables
# Copy this to .env.test.local for testing

# Sanity Configuration (use test dataset)
NEXT_PUBLIC_SANITY_PROJECT_ID=test-project-id
NEXT_PUBLIC_SANITY_DATASET=test
SANITY_API_TOKEN=test-token

# Site Configuration
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Email Service (use Resend test domain)
RESEND_API_KEY=test-resend-key
EMAIL_FROM=onboarding@resend.dev
EMAIL_API_KEY=test-api-key
WEBHOOK_SECRET=test-webhook-secret

# Google Analytics (optional for tests)
NEXT_PUBLIC_GA_ID=G-TEST123
